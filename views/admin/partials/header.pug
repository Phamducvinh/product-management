header.header
    .container-fluid
        .row.align-items-center
            .col-3
                .inner-logo
                    a(href="/admin/dashboard") Admin Dashboard

            .col-9
                div(class="text-right")
                    a(
                        href=`${prefixAdmin}/my-account`
                        class="btn btn-primary btm-sm mr-2" 
                    ) #{user.fullName}
                    a(
                        href=`${prefixAdmin}/auth/logout`
                        class="btn btn-danger"
                    ) Đăng xuất